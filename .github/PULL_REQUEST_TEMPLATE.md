## What & why

Describe the change and the problem it solves.

## Checklist

- [ ] Single-file / dependency-light kept intact
- [ ] `?test=1` self-check passes (and I added an assert for new non-trivial logic)
- [ ] Bumped `CACHE` in `sw.js` if the app shell changed
- [ ] Tested in a browser (note which)
